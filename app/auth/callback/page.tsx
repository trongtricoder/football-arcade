import { AuthCallback } from "./auth-callback";

export const metadata = { title: "Securing account", robots: { index: false, follow: false } };

export default function AuthCallbackPage() {
  return <AuthCallback />;
}

